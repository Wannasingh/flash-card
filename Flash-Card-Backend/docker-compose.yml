services:
  backend:
    build: .
    ports:
      - "8081:8081"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/v3/api-docs"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
